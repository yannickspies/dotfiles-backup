# Secrets - NEVER commit these
.env
.credentials.json

# Runtime data
history.jsonl
debug/
todos/
projects/
session-env/
shell-snapshots/
statsig/
plugins/
data/
file-history/
plans/
sessions/
teams/
.last-cleanup
.last-update-result.json
mcp-needs-auth-cache.json

# Logs (tracked at repo root in dotfiles/logs/)
logs/

# Build/runtime caches
cache/
paste-cache/
stats-cache.json
tasks/
telemetry/
backups/

# Background daemon + job queue (machine-local runtime)
daemon/
daemon.lock
daemon.log
daemon.status.json
jobs/

# Machine-specific overrides
# settings.local.json is NOT tracked - it holds per-machine permission
# grants and absolute paths. Create it by hand on each machine.
settings.local.json
